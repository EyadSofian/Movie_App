import 'package:flutter/material.dart';

class MovieSearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('MovieSearchPage')),
      body: Center(child: Text('MovieSearchPage')),
    );
  }
}
