import 'package:flutter/material.dart';

class MovieBrowsePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('MovieBrowsePage')),
      body: Center(child: Text('MovieBrowsePage')),
    );
  }
}